"""改进递归图（论文3 / 阶段 M）— 占位。"""


def build_modified_rp(signal, method_cfg):
    raise NotImplementedError(
        "method.representation=mrp 属于阶段 M，尚未实现。"
        "请将 config 中 method.representation 设为 rp。"
    )
